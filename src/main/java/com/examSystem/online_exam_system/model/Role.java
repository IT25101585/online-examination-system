package com.examSystem.online_exam_system.model;

//enum is a special type that holds a fixed set off constants
public enum Role {
    ADMIN,      //can manage everything
    TEACHER,    //can create exams and questions
    STUDENT     //can take exam and view results
}
